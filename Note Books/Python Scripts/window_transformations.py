# Databricks notebook source
# MAGIC %md
# MAGIC
# MAGIC sum() → Adds values within the group.
# MAGIC avg() → Computes group average.
# MAGIC max() → Finds maximum value.
# MAGIC min() → Finds minimum value.
# MAGIC count() → Number of rows in each group.
# MAGIC avg() → Average of values.
# MAGIC max() → Maximum value in group.
# MAGIC concat_ws() → Concatenates string values within a column, separated by a delimiter (,).
# MAGIC first() → Retrieves the first value of a column in a group.
# MAGIC last() → Retrieves the last value of a column in a group.
# MAGIC stddev() → Standard deviation of values.
# MAGIC variance() → Variance of values.
# MAGIC .alias() → Rename the result columns after aggregation.
# MAGIC sumDistinct() → Sums only unique values in a column (avoids double-counting duplicates).

# COMMAND ----------


